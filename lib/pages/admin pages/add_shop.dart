import 'package:flutter/material.dart';

class AddShopPage extends StatelessWidget {
  const AddShopPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("AddShopPage"),
    );
  }
}
